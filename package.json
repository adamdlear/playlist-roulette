{
    "name": "playlist-roulette",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "sst dev",
        "deploy": "sst deploy",
        "remove": "sst remove",
        "format": "prettier --write ."
    },
    "dependencies": {
        "sst": "3.5.7"
    },
    "devDependencies": {
        "@types/node": "^20",
        "prettier": "^3.4.2",
        "typescript": "^5"
    },
    "workspaces": [
        "web"
    ],
    "prettier": {
        "tabWidth": 4
    }
}
